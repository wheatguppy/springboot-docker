// 流水线
pipeline {
    // 	在任何可用的代理上，执行流水线或它的任何阶段
    agent any

    // 阶段
    stages {
        // 定义 "Build" 阶段
        stage ('Build') {
            // 	执行与 "Build" 阶段相关的步骤
            steps {

            }
        }
        // 定义 "Test" 阶段
        stage ('Test') {
            // 	执行与 "Test" 阶段相关的步骤
            steps {

            }
        }
        // 定义 "Deploy" 阶段
        stage ('Deploy') {
            // 	执行与 "Deploy" 阶段相关的步骤
            steps {

            }
        }
    }
}